{
  "name": "interval-polling",
  "version": "1.0.2",
  "description": "Interval polling",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alexandrij/interval-polling.git"
  },
  "files": [
    "/dist",
    "index.d.ts"
  ],
  "main": "dist/interval-polling.js",
  "types": "./dist/interval-polling.d.ts",
  "engines": {
    "node": ">= 10.0.0",
    "npm": ">= 6.0.0"
  },
  "scripts": {
    "build": "npm run clean && npm run compile",
    "compile": "tsc",
    "clean": "rm -rf ./dist",
    "test": "jest"
  },
  "keywords": [
    "polling"
  ],
  "author": "adimitriev",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "jest": "^27.5.0",
    "ts-jest": "^27.1.3",
    "typescript": "^4.5.5"
  }
}
